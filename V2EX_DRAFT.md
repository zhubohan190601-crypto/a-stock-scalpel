# V2EX 发文草稿 — 建议发布后第2-3天发

> 标题：开源了一个 A 股量化评分工具，规则完全透明（9 个因子 + 32 条规则表）

---

## 正文

之前折腾 A 股短线分析，试了几个 AI 选股工具，发现全是黑盒——你不知道它为什么打分，也不知道它的逻辑稳不稳定。

所以自己写了个完全透明的多因子评分系统，核心思路很简单：

**9 个技术因子 × 32 条确定性规则 = 净得分 + 信号**

因子：RSI、MACD、KDJ、布林带、一目均衡、鳄鱼线、ADX、OBV、量比

规则：比如「均线多头排列 +2 分」「RSI 超买 -2 分」——全部写在一个 `rules.py` 里，你可以打开看，改，审计。

**数据源**用新浪财经+东方财富免费的，零成本，不需要 API Key。还做了自动 fallback，一个挂了一个顶上。

**回测**也做了，摩擦成本按 0.457% 算（印花税+佣金+滑点），短线策略不把摩擦成本算进去的话回测虚高 20-40%。

架构就四层文件：

```
fetcher.py → 采集
analyzer.py → 因子计算
rules.py → 规则评分
backtest.py → 回测
```

60 秒跑完 80 只沪深龙头，命令行直接输出排名。

**GitHub**: https://github.com/zhubohan190601-crypto/a-stock-scalpel

⚠️ 免责：纯研究工具，不构成投资建议。

欢迎 PR 加因子或者改规则参数。
